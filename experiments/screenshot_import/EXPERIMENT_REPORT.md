# 实验执行记录

执行环境：Python 3.13.5、OpenCV 4.13.0、NumPy 2.3.5、Pillow 12.2.0、pytest 9.0.2。

## 可重复 mock 端到端结果

| 样本 | 网格 | 课程块 | 待确认字段 | 总耗时 |
|---|---:|---:|---:|---:|
| `standard_10` | 7 列 / 10 行，置信度 0.9849 | 5/5 | 4 | 0.226 s |
| `tilted_12` | 7 列 / 12 行，置信度 0.9762 | 4/4 | 3 | 0.309 s |

`standard_10` 正确定位两节课程、单双周、缺失地点和同一星期相邻课程块；`tilted_12` 正确定位三个跨三节课程和一个两节课程，并校正约 1.8° 倾斜。

待确认项来自刻意设置的 0.88 课程名、没有“老师”后缀的 `李明`、以及两个缺失地点。扁平最终值与证据状态保持分离。

上述耗时使用 fixture OCR，不代表 PaddleOCR CPU 推理耗时。

## PaddleOCR 实测状态

未运行，不能报告真实文本识别准确率和 CPU 推理耗时。

阻塞记录：

1. 内部镜像安装 `paddlepaddle==3.3.1 paddleocr==3.7.0`：无可用发行包；
2. Paddle 官方 CPU 源安装 `paddlepaddle==3.3.0`：域名解析失败；
3. 单独安装 `paddleocr==3.7.0`：内部镜像无可用发行包。

## 自动化测试

本地 `pytest`：26 项全部通过，覆盖图片格式/EXIF/缩放/倾斜、10/12 节网格、课程块、空白网格、明确失败、字段规则、mock OCR、ImportDraft V2 映射、归一化坐标、Windows Rust 子进程 UTF-8 解码和 CLI UTF-8 控制台配置。
