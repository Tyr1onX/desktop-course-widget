{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "presentation",
  "description": "Allows the presentation controller to read the schedule, exchange replay events, and hide its own window.",
  "windows": [
    "presentation"
  ],
  "permissions": [
    "core:event:default",
    "core:window:allow-hide",
    "allow-read-schedule"
  ]
}
